此程序不能得到预期的结果。
此程序是希望通过强制类型转换将Derived对象指针转换为Base2对象指针。
而以此程序的方法则没有遵循派生类向基类转换时的对应法则。它这样转换并不会隐式地调整基类指针的位置，从而出现错误。

正确的用法：
1.直接将派生类指针赋给基类指针，这样可以隐式地调整指针的值。
2.显式调整指针的值，自己加上该基类指针所应偏移的值。

明显第二种很不好用，一不小心就会出现错误，因此用第一种比较好。